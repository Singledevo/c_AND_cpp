/*find the pivot element in the array 
the pivot element are the element which [4, 5, 6, 7, 0, 1, 2], 
the pivot is 0 because it is the highest point where the array transitions from increasing to decreasing. */
#include<iostream>
using namespace std;

class 
int main()
{

    return 0;
}